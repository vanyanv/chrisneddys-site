# Google Analytics 4 measurement ID, e.g. G-XXXXXXXXXX.
# Set this in the Vercel project's environment variables for Production.
# Leave it unset locally so dev and preview traffic stays out of the property.
NEXT_PUBLIC_GA_ID=
