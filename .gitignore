node_modules/
.next/
out/
.env
.env.local

# Local dev database (PGlite), file-persisted so `pnpm dev` survives restarts.
.pglite/
.DS_Store
*.log
.vscode/
.idea/

# Local scratch: Playwright MCP captures and TS incremental cache
.playwright-mcp/
tsconfig.tsbuildinfo

# Claude Code agent worktrees
.claude/worktrees/
.vercel
